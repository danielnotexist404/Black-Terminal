# Python Indicator Examples

These scripts document the planned indicator shape. They are not executed by the app yet.

The runtime will call a `compute(ctx)` function and expect a JSON-serializable result containing
plots, optional signals, and optional diagnostics.

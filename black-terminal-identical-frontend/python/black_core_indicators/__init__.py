"""Black Core research-grade indicator engines.

These modules are deterministic analysis references. Browser production rendering
uses the TypeScript worker mirror until a separately deployed Python runtime is
certified and connected.
"""

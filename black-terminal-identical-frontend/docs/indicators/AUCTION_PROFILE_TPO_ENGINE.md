# Auction Profile TPO Engine

TPO counts unique time brackets that visited each price row. Bracket duration is configurable and the same deterministic grid and scope system are shared with CVD and volume.

TPO POC is the row with the greatest bracket count. TPO can be selected independently for value area and node detection. TPO single-print candidates are classified separately from CVD LVNs; they are not presented as equivalent structures.
